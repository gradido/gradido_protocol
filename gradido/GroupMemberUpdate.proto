syntax = "proto3";

package proto.gradido;

message GroupMemberUpdate {
    enum MemberType {
        PRIVATE = 0;
        COMPANY = 1;
        SUBACCOUNT = 2;
    };

    bytes user_pubkey = 1;
    MemberType member_type = 2;
    bytes member_hash = 3;   
}